<html>
   <body>

      <table>
      <tr><th>Name</th></tr>
      <% for (var i = 0; i < c.length; i++) { %>
        <tr>
          <td><%= c[i].name %></td>
        </tr>
      <% } %>
    </table>

   </body>
</html>
